---
title: "veri evals"
sidebarTitle: "Evals"
description: "CLI for defining and running evaluations. Planned for a later release."
keywords: ["veri evals", "evaluation", "scorer", "cli"]
---

<Note>
  **Work in progress.** The `veri evals` command group is planned for a later release and is not available in the CLI yet.
</Note>

## What the CLI will do

Once evaluations ship, `veri evals` will let you define an eval (a dataset plus scorers), run it against a deployed model, and inspect per-sample outputs and aggregate scores from the terminal. The commands and flags will be documented here at release.
