---
title: "Datasets"
description: "Upload and manage training datasets"
---

## Upload Dataset

<ParamField query="name" type="string" required>
  A human-readable name for the dataset.
</ParamField>

<ParamField body="file" type="file" required>
  A JSONL file where each line is a JSON object containing a `prompt` field.
</ParamField>

### Request

```bash
curl -X POST "http://localhost:8000/v1/datasets?name=math_prompts" \
  -H "Authorization: Bearer $VERI_INTERNAL_API_KEY" \
  -H "X-Veri-User-Id: $VERI_USER_ID" \
  -F "file=@math_prompts.jsonl"
```

### Response

```json
{
  "id": "ds_abc123",
  "user_id": "user_123",
  "name": "math_prompts",
  "source_type": "upload",
  "s3_key": "datasets/ds_abc123/data.jsonl",
  "source_uri": null,
  "hf_dataset": null,
  "num_rows": 1500,
  "created_at": "2026-04-14T12:00:00Z"
}
```

---

## Connect Dataset

<ParamField body="name" type="string" required>
  Display name for the connected dataset.
</ParamField>

<ParamField body="source_type" type="string" required>
  One of `s3`, `gs`, `az`, `hf`, `postgres`, `mysql`, `snowflake`, or `bigquery`.
</ParamField>

<ParamField body="source_uri" type="string">
  Required for `s3`, `gs`, and `az` sources.
</ParamField>

<ParamField body="hf_dataset" type="string">
  Required for `hf` sources.
</ParamField>

<ParamField body="hf_config" type="object">
  Optional Hugging Face config, including `split`, `column_mapping`, and `token`.
</ParamField>

<ParamField body="db_connection" type="string">
  Required for SQL, Snowflake, and BigQuery-backed sources.
</ParamField>

<ParamField body="db_query" type="string">
  Required for database-backed sources.
</ParamField>

<ParamField body="credentials" type="object">
  Optional source credentials for validation. These are used to test the connection but are **not stored** — you will need to provide them again at training time if the source requires authentication.
</ParamField>

### Request

```bash
curl -X POST http://localhost:8000/v1/datasets/connect \
  -H "Authorization: Bearer $VERI_INTERNAL_API_KEY" \
  -H "X-Veri-User-Id: $VERI_USER_ID" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "gsm8k-train",
    "source_type": "hf",
    "hf_dataset": "gsm8k",
    "hf_config": {
      "split": "train",
      "column_mapping": {
        "question": "prompt",
        "answer": "label"
      }
    }
  }'
```

### Response

```json
{
  "id": "ds_hf123456789",
  "user_id": "user_123",
  "name": "gsm8k-train",
  "source_type": "hf",
  "s3_key": null,
  "source_uri": null,
  "hf_dataset": "gsm8k",
  "num_rows": null,
  "created_at": "2026-04-21T12:00:00Z"
}
```

---

## Validate Dataset Connection

This endpoint tests a source and returns preview metadata without creating a dataset row.

### Request

```bash
curl -X POST http://localhost:8000/v1/datasets/connect/validate \
  -H "Authorization: Bearer $VERI_INTERNAL_API_KEY" \
  -H "X-Veri-User-Id: $VERI_USER_ID" \
  -H "Content-Type: application/json" \
  -d '{
    "source_type": "s3",
    "source_uri": "s3://my-bucket/prompts.jsonl"
  }'
```

### Response

```json
{
  "valid": true,
  "num_rows": 1500,
  "columns": ["prompt", "expected_answer"],
  "error": null
}
```

---

## List Datasets

<ParamField query="limit" type="integer" default="20">
  Maximum number of datasets to return.
</ParamField>

<ParamField query="offset" type="integer" default="0">
  Number of datasets to skip.
</ParamField>

### Request

```bash
curl "http://localhost:8000/v1/datasets?limit=10&offset=0" \
  -H "Authorization: Bearer $VERI_INTERNAL_API_KEY" \
  -H "X-Veri-User-Id: $VERI_USER_ID"
```

### Response

```json
{
  "items": [
    {
      "id": "ds_abc123",
      "name": "math_prompts",
      "source_type": "upload",
      "s3_key": "datasets/ds_abc123/data.jsonl",
      "source_uri": null,
      "hf_dataset": null,
      "num_rows": 1500,
      "created_at": "2026-04-14T12:00:00Z"
    },
    {
      "id": "ds_def456",
      "name": "gsm8k-train",
      "source_type": "hf",
      "s3_key": null,
      "source_uri": null,
      "hf_dataset": "gsm8k",
      "num_rows": null,
      "created_at": "2026-04-21T12:00:00Z"
    }
  ],
  "total": 2,
  "limit": 10,
  "offset": 0
}
```

---

## Get Dataset

### Request

```bash
curl http://localhost:8000/v1/datasets/ds_abc123 \
  -H "Authorization: Bearer $VERI_INTERNAL_API_KEY" \
  -H "X-Veri-User-Id: $VERI_USER_ID"
```

### Response

```json
{
  "id": "ds_abc123",
  "name": "math_prompts",
  "source_type": "upload",
  "s3_key": "datasets/ds_abc123/data.jsonl",
  "source_uri": null,
  "hf_dataset": null,
  "num_rows": 1500,
  "created_at": "2026-04-14T12:00:00Z"
}
```

## Dataset Format

Each uploaded JSONL row should be a JSON object with at least a `prompt` field:

```json
{"prompt": "What is 15 * 23?"}
{"prompt": "Solve for x: 2x + 5 = 17"}
{"prompt": "Explain the chain rule in calculus."}
```

The `prompt` value can be either a string or a list of chat messages. Additional fields stay available to reward functions and data resolvers.

<Note>
Connected datasets are resolved at job start, so validation is the best way to catch auth or schema issues early.
</Note>
