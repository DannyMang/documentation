---
title: "Reward Functions"
description: "Upload and manage Python reward functions"
---

## Upload Reward Function

<ParamField body="name" type="string" required>
  A human-readable name for the reward function.
</ParamField>

<ParamField body="file" type="file" required>
  A Python `.py` file containing the reward function.
</ParamField>

<ParamField body="format" type="string" required>
  The reward function format. Supported values: `trl` and `miles`.
</ParamField>

### Request

```bash
curl -X POST http://localhost:8000/v1/reward_functions \
  -H "Authorization: Bearer $VERI_INTERNAL_API_KEY" \
  -H "X-Veri-User-Id: $VERI_USER_ID" \
  -F "name=math_reward" \
  -F "file=@reward.py" \
  -F "format=trl"
```

### Response

```json
{
  "id": "rf_def456",
  "user_id": "user_123",
  "name": "math_reward",
  "s3_key": "rewards/rf_def456/reward.py",
  "format": "trl",
  "created_at": "2026-04-14T12:05:00Z"
}
```

---

## List Reward Functions

<ParamField query="limit" type="integer" default="20">
  Maximum number of reward functions to return.
</ParamField>

<ParamField query="offset" type="integer" default="0">
  Number of reward functions to skip.
</ParamField>

### Request

```bash
curl "http://localhost:8000/v1/reward_functions?limit=10&offset=0" \
  -H "Authorization: Bearer $VERI_INTERNAL_API_KEY" \
  -H "X-Veri-User-Id: $VERI_USER_ID"
```

### Response

```json
{
  "items": [
    {
      "id": "rf_def456",
      "name": "math_reward",
      "s3_key": "rewards/rf_def456/reward.py",
      "format": "trl",
      "created_at": "2026-04-14T12:05:00Z"
    }
  ],
  "total": 1,
  "limit": 10,
  "offset": 0
}
```

---

## Get Reward Function

### Request

```bash
curl http://localhost:8000/v1/reward_functions/rf_def456 \
  -H "Authorization: Bearer $VERI_INTERNAL_API_KEY" \
  -H "X-Veri-User-Id: $VERI_USER_ID"
```

### Response

```json
{
  "id": "rf_def456",
  "name": "math_reward",
  "s3_key": "rewards/rf_def456/reward.py",
  "format": "trl",
  "created_at": "2026-04-14T12:05:00Z"
}
```

## Reward Function Format

### `trl`

`trl` reward files must contain `def reward(`. The control plane currently performs a lightweight signature check before accepting the file.

```python
def reward(completions, answer=None, **kwargs):
    return [1.0 if answer and str(answer) in c else 0.0 for c in completions]
```

### `miles`

`miles` reward files must contain `async def reward(`:

```python
async def reward(args, sample, **kwargs) -> float:
    if sample.label and str(sample.label) in sample.response:
        return 1.0
    return 0.0
```

<Note>
Uploads are syntax-checked with `compile(...)` before they are stored.
</Note>

See [Reward Functions](/concepts/reward-functions) for a more detailed guide.
